name: Build & deploy

on:
  push:
    branches: [dependant]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
      - name: Setup Node.js environment
        uses: actions/setup-node@v6.2.0
      - name: Running build
        run: pnpm run build
  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: second job 1 step
        run: echo "second job 1"
      - name: second 2
        run: echo "second 2"
